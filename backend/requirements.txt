fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4는 bcrypt>=4.1(72바이트 초과 시 ValueError로 동작 변경)과 호환되지 않아
# 내부 자체 점검(detect_wrap_bug)에서 죽는다. 4.1 미만으로 고정한다.
bcrypt==4.0.1
python-multipart==0.0.9
httpx==0.27.2
pytest==8.3.3
